{
  "name": "smart-charging-platform",
  "version": "2.0.0",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": ["apps/*"],
  "scripts": {
    "build:web": "npm run build --workspace=@smartcharge/admin-web",
    "check:weapp": "npm run check --workspace=@smartcharge/wechat-miniapp",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  },
  "overrides": {
    "lodash": "4.18.1"
  }
}
