spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: ''
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost/test
